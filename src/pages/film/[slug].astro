---
import Base from '@/layouts/Base.astro';
import { getCollection, type CollectionEntry } from 'astro:content';

export async function getStaticPaths() {
  const items = await getCollection('film');
  return items.map(item => ({
    params: { slug: item.slug },
    props: { item },
  }));
}

interface Props {
  item: CollectionEntry<'film'>;
}

const { item } = Astro.props;
const { Content } = await item.render();

function formatDate(date: Date): string {
  return date.toLocaleDateString('en-US', { 
    year: 'numeric',
    month: 'long', 
    day: 'numeric' 
  });
}
---

<Base 
  title={`${item.data.title}${item.data.director ? ` (${item.data.director})` : ''}`}
  description={item.data.description}
>
  <article class="container container--narrow">
    <nav class="breadcrumbs animate-in">
      <a href="/">Home</a>
      <span class="breadcrumbs__separator">/</span>
      <a href="/film">Film</a>
      <span class="breadcrumbs__separator">/</span>
      <span class="breadcrumbs__current">{item.data.title}</span>
    </nav>
    
    <header class="film-header animate-in">
      <h1 class="film-header__title">{item.data.title}</h1>
      {item.data.director && (
        <p class="film-header__director">Directed by {item.data.director}</p>
      )}
      
      <div class="film-header__meta">
        {item.data.year && <span>{item.data.year}</span>}
        {item.data.genre.length > 0 && (
          <div class="tags">
            {item.data.genre.map(g => (
              <span class="tag">{g}</span>
            ))}
          </div>
        )}
      </div>
      
      {item.data.rating && (
        <div class="film-header__rating">
          <span class="rating-value">{item.data.rating.toFixed(1)}</span>
          <span class="rating-max">/ 10</span>
        </div>
      )}
    </header>

    <div class="prose animate-in">
      <Content />
    </div>
    
    <footer class="article-footer">
      {item.data.watch_link && (
        <div class="footer-links">
          <a href={item.data.watch_link} target="_blank" rel="noopener" class="external-link">
            Watch
          </a>
        </div>
      )}
      
      <p class="meta-note">Added {formatDate(item.data.date)}</p>
      
      <nav class="article-nav">
        <a href="/film" class="article-nav__link">
          <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="m12 19-7-7 7-7"></path>
            <path d="M19 12H5"></path>
          </svg>
          Back to film
        </a>
      </nav>
    </footer>
  </article>
</Base>

<style>
  .film-header {
    margin-bottom: var(--space-3xl);
    padding-bottom: var(--space-2xl);
    border-bottom: 1px solid var(--border);
  }
  
  .film-header__title {
    font-size: var(--font-size-h1);
    font-weight: var(--font-weight-heading);
    line-height: var(--line-height-h1);
    margin: 0 0 var(--space-sm) 0;
  }

  .film-header__director {
    font-size: var(--font-size-h2);
    color: var(--text-secondary);
    margin: 0 0 var(--space-lg) 0;
  }

  .film-header__meta {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    font-size: var(--font-size-mono);
    font-family: var(--font-mono);
    color: var(--text-tertiary);
    margin-bottom: var(--space-lg);
  }

  .film-header__rating {
    display: inline-flex;
    align-items: baseline;
    gap: var(--space-xs);
  }

  .rating-value {
    font-family: var(--font-serif);
    font-size: var(--font-size-h1);
    font-weight: var(--font-weight-heading);
    color: var(--accent);
  }

  .rating-max {
    font-size: var(--font-size-ui);
    color: var(--text-tertiary);
  }

  .article-footer {
    margin-top: var(--space-4xl);
    padding-top: var(--space-xl);
    border-top: 1px solid var(--border);
  }

  .footer-links {
    margin-bottom: var(--space-lg);
  }

  .external-link {
    display: inline-flex;
    align-items: center;
    gap: var(--space-sm);
    font-size: var(--font-size-ui);
    color: var(--text-secondary);
    padding: var(--space-sm) var(--space-md);
    background: var(--bg-tertiary);
    border-radius: 6px;
    transition: background-color var(--transition-fast), color var(--transition-fast);
  }

  .external-link:hover {
    background: var(--accent);
    color: white;
  }

  .meta-note {
    font-size: var(--font-size-mono);
    font-family: var(--font-mono);
    color: var(--text-tertiary);
    margin-bottom: var(--space-xl);
  }

  .article-nav__link {
    display: inline-flex;
    align-items: center;
    gap: var(--space-sm);
    font-size: var(--font-size-ui);
    color: var(--text-secondary);
    text-decoration: none;
    padding: var(--space-sm) var(--space-md);
    margin-left: calc(-1 * var(--space-md));
    border-radius: 6px;
    transition: background-color var(--transition-fast), color var(--transition-fast);
  }
  
  .article-nav__link:hover {
    background: var(--bg-tertiary);
    color: var(--text-primary);
  }
</style>
