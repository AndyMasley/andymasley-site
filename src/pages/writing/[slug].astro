---
import Base from '@/layouts/Base.astro';
import { getCollection } from 'astro:content';
import { fetchSubstackPosts, fetchPostContent, processPostContent, type SubstackPost } from '@/lib/substack';
import { fetchEAForumPosts, fetchEAForumPostContent, processEAForumContent, type EAForumPost } from '@/lib/eaforum';
import { getMetaPostSlugs } from '@/lib/meta-posts';
import { CheatSheetExplorer } from '@/components/CheatSheetExplorer';

// Union type for all post sources
type PostSource = 'local' | 'substack' | 'eaforum';

interface LocalPost {
  source: 'local';
  slug: string;
  title: string;
  date: Date;
  description: string;
  tags: string[];
  body: string;
  render: () => Promise<{ Content: any; headings: any[] }>;
}

interface ExternalPost {
  source: 'substack' | 'eaforum';
  slug: string;
  title: string;
  date: Date;
  description: string;
  url: string;
  htmlContent: string;
  sourceName: string;
}

type Post = LocalPost | ExternalPost;

export async function getStaticPaths() {
  const paths: Array<{ params: { slug: string }; props: { post: Post } }> = [];

  // 1. Get local content collection posts
  const localPosts = await getCollection('writing');
  for (const post of localPosts) {
    paths.push({
      params: { slug: post.slug },
      props: {
        post: {
          source: 'local' as const,
          slug: post.slug,
          title: post.data.title,
          date: post.data.date,
          description: post.data.description || '',
          tags: post.data.tags || [],
          body: post.body,
          render: () => post.render(),
        }
      }
    });
  }

  // 2. Get Substack posts
  const metaSlugs = getMetaPostSlugs();
  const substackPosts = await fetchSubstackPosts();

  for (const post of substackPosts) {
    // Skip meta posts and posts that conflict with local content
    if (metaSlugs.includes(post.slug)) continue;
    if (paths.some(p => p.params.slug === post.slug)) continue;

    // Fetch full content
    const rawContent = await fetchPostContent(post.slug);
    const htmlContent = processPostContent(rawContent, post.slug);

    paths.push({
      params: { slug: post.slug },
      props: {
        post: {
          source: 'substack' as const,
          slug: post.slug,
          title: post.title,
          date: post.date,
          description: post.description,
          url: post.url,
          htmlContent,
          sourceName: 'Substack',
        }
      }
    });
  }

  // 3. Get EA Forum posts
  const eaForumPosts = await fetchEAForumPosts();

  for (const post of eaForumPosts) {
    // Skip posts that conflict with existing paths
    if (paths.some(p => p.params.slug === post.slug)) continue;

    // Fetch full content
    const rawContent = await fetchEAForumPostContent(post.postId);
    const htmlContent = processEAForumContent(rawContent, post.slug);

    paths.push({
      params: { slug: post.slug },
      props: {
        post: {
          source: 'eaforum' as const,
          slug: post.slug,
          title: post.title,
          date: post.date,
          description: '',
          url: post.url,
          htmlContent,
          sourceName: 'EA Forum',
        }
      }
    });
  }

  return paths;
}

interface Props {
  post: Post;
}

const { post } = Astro.props;

// Format date as MM.DD.YYYY
function formatDate(date: Date): string {
  const month = String(date.getMonth() + 1).padStart(2, '0');
  const day = String(date.getDate()).padStart(2, '0');
  const year = date.getFullYear();
  return `${month}.${day}.${year}`;
}

// Estimate reading time
function getReadingTime(content: string): number {
  const text = content.replace(/<[^>]*>/g, ''); // Strip HTML
  const wordCount = text.split(/\s+/).length;
  return Math.ceil(wordCount / 200);
}

// Get content and metadata based on source
let Content: any = null;
let readingTime = 0;

if (post.source === 'local') {
  const rendered = await post.render();
  Content = rendered.Content;
  readingTime = Math.ceil(post.body.split(/\s+/).length / 200);
} else {
  readingTime = getReadingTime(post.htmlContent);
}

const isCheatSheet = post.slug === 'a-cheat-sheet-for-conversations-about';
const wordCount = post.source === 'local'
  ? post.body.split(/\s+/).length
  : (post.htmlContent.replace(/<[^>]*>/g, '').split(/\s+/).length);
---

{isCheatSheet && post.source !== 'local' ? (
<Base
  title={post.title}
  description="Every environmental objection to using ChatGPT, answered with data and sources."
>
  <div class="cheat-sheet-page">
    <p class="originally-on">
      Originally on <a href={post.url} target="_blank" rel="noopener">{post.sourceName}</a>
    </p>

    <header class="article-header">
      <h1 class="article-header__title">{post.title}</h1>
      <div class="article-header__meta">
        <span>{formatDate(post.date)}</span>
        <span>{readingTime} min read</span>
        <span>{Math.round(wordCount / 1000)}k words</span>
      </div>
    </header>

    <CheatSheetExplorer client:load html={post.htmlContent} />

    <footer class="article-footer">
      <a href="/writing" class="article-nav__link">&larr; Back to all writing</a>
    </footer>
  </div>
</Base>
) : (
<Base
  title={post.title}
  description={post.description}
>
  <article class="container container--narrow">

    {post.source !== 'local' && (
      <p class="originally-on">
        Originally on <a href={post.url} target="_blank" rel="noopener">{post.sourceName}</a>
      </p>
    )}

    <header class="article-header">
      <h1 class="article-header__title">{post.title}</h1>
      <div class="article-header__meta">
        <span class="article-header__date">{formatDate(post.date)}</span>
        <span class="article-header__reading-time">{readingTime} min read</span>
      </div>
    </header>

    <div class="prose">
      {post.source === 'local' ? (
        <Content />
      ) : (
        <Fragment set:html={post.htmlContent} />
      )}
    </div>

    <footer class="article-footer">
      <nav class="article-nav">
        <a href="/writing" class="article-nav__link">
          ← Back to all writing
        </a>
      </nav>
    </footer>
  </article>
</Base>
)}

<style>
  .originally-on {
    font-size: var(--font-size-ui);
    color: var(--dim);
    margin-bottom: var(--space-lg);
  }

  .originally-on a {
    color: var(--accent);
    text-decoration: underline;
    text-underline-offset: var(--underline-offset);
  }

  .article-header {
    margin-bottom: var(--space-xl);
  }

  .article-header__title {
    font-size: 2.25rem;   /* 36px */
    font-weight: 700;
    line-height: 1.15;
    letter-spacing: -0.03em;
    margin-bottom: var(--space-md);
  }

  @media (max-width: 600px) {
    .article-header__title {
      font-size: 1.75rem;   /* 28px */
    }
  }

  .article-header__meta {
    display: flex;
    gap: var(--space-md);
    font-size: var(--font-size-mono);
    font-family: var(--font-mono);
    color: var(--dim);
  }

  .article-footer {
    margin-top: var(--space-2xl);
    padding-top: var(--space-xl);
    border-top: 1px solid var(--border);
  }

  .article-nav__link {
    font-size: var(--font-size-ui);
    color: var(--accent);
    text-decoration: underline;
    text-underline-offset: var(--underline-offset);
  }

  /* Prose styles for external content */
  .prose :global(img) {
    max-width: 100%;
    height: auto;
    margin: var(--space-lg) 0;
  }

  .prose :global(figure) {
    margin: var(--space-lg) 0;
  }

  .prose :global(figcaption) {
    font-size: var(--font-size-ui);
    color: var(--dim);
    text-align: center;
    margin-top: var(--space-sm);
  }

  .prose :global(blockquote) {
    border-left: 3px solid var(--accent);
    padding-left: var(--space-md);
    margin: var(--space-lg) 0;
    color: var(--dim);
    font-style: italic;
  }

  .prose :global(pre) {
    background: var(--border);
    padding: var(--space-md);
    border-radius: 4px;
    overflow-x: auto;
    font-family: var(--font-mono);
    font-size: var(--font-size-mono);
  }

  .prose :global(code) {
    font-family: var(--font-mono);
    font-size: var(--font-size-mono);
    background: var(--border);
    padding: 0.1em 0.3em;
    border-radius: 2px;
  }

  .prose :global(pre code) {
    background: none;
    padding: 0;
  }

  .prose :global(a) {
    color: var(--accent);
    text-decoration: underline;
    text-underline-offset: var(--underline-offset);
  }

  .prose :global(h2) {
    font-size: 1.5rem;      /* 24px */
    font-weight: 600;
    line-height: 1.3;
    margin-top: 2.5rem;     /* 40px - clear section break */
    margin-bottom: 1rem;    /* 16px - breathable but connected */
  }

  .prose :global(h3) {
    font-size: 1.25rem;     /* 20px */
    font-weight: 600;
    line-height: 1.35;
    margin-top: 2rem;       /* 32px */
    margin-bottom: 0.75rem; /* 12px */
  }

  .prose :global(h4) {
    font-size: 1.125rem;    /* 18px */
    font-weight: 600;
    line-height: 1.4;
    margin-top: 1.5rem;     /* 24px */
    margin-bottom: 0.5rem;  /* 8px */
  }

  .prose :global(p) {
    margin-bottom: var(--space-md);
  }

  .prose :global(ul),
  .prose :global(ol) {
    margin-bottom: var(--space-md);
    padding-left: var(--space-lg);
  }

  .prose :global(li) {
    margin-bottom: var(--space-sm);
  }

  /* Substack-specific styles */
  .prose :global(.image-link),
  .prose :global(.image2) {
    display: block;
    margin: var(--space-lg) 0;
  }

  .prose :global(.image-link img),
  .prose :global(.image2 img) {
    max-width: 100%;
    height: auto;
  }

  .prose :global(.captioned-image-container) {
    margin: var(--space-lg) 0;
  }

  .prose :global(.image-caption) {
    font-size: var(--font-size-ui);
    color: var(--dim);
    text-align: center;
    margin-top: var(--space-sm);
  }

  /* Hide Substack subscription widgets and image controls */
  .prose :global(.subscription-widget-wrap),
  .prose :global(.subscribe-widget),
  .prose :global(.subscription-widget),
  .prose :global(.image-link-expand),
  .prose :global(.icon-container) {
    display: none !important;
  }

  /* Footnote styling - inline references */
  .prose :global(sup) {
    font-size: 0.7em;
    line-height: 0;
    vertical-align: baseline;
    position: relative;
    top: -0.4em;
  }

  .prose :global(sup a) {
    font-size: inherit;
    text-decoration: none;
    color: var(--accent);
  }

  .prose :global(sup a:hover) {
    text-decoration: underline;
  }

  /* Substack-specific footnote anchors (inline "1" references) */
  .prose :global(.footnote-anchor) {
    font-size: 0.7em;
    vertical-align: super;
    line-height: 0;
    text-decoration: none;
    color: var(--accent);
  }

  .prose :global(.footnote-anchor:hover) {
    text-decoration: underline;
  }

  /* Footnote section at bottom */
  .prose :global(.footnotes),
  .prose :global(.footnote) {
    font-size: var(--font-size-ui);
    color: var(--dim);
    margin-top: var(--space-xl);
    padding-top: var(--space-md);
    border-top: 1px solid var(--border);
  }

  .prose :global(.footnote .footnote-number) {
    font-size: 0.85em;
    margin-right: var(--space-sm);
    color: var(--accent);
    text-decoration: none;
  }

  .prose :global(.footnotes li),
  .prose :global(.footnote li) {
    margin-bottom: var(--space-sm);
  }

  /* ═══ Cheat Sheet Layout ═══ */
  :global(.main-content:has(.cheat-sheet-page)) {
    max-width: 1200px;
    padding-left: 32px;
    padding-right: 32px;
  }
  .cheat-sheet-page { max-width: 100%; }

  :global(.cse-progress-bar) {
    position: fixed; top: 0; left: 0; right: 0; height: 3px;
    z-index: 1000; background: transparent; pointer-events: none;
  }
  :global(.cse-progress-fill) {
    height: 100%; background: var(--accent); transition: width 0.1s linear;
  }
  :global(.cse-layout) {
    display: grid; grid-template-columns: 240px 1fr; gap: 3rem; align-items: start;
  }
  @media (max-width: 900px) {
    :global(.cse-layout) { grid-template-columns: 1fr; }
  }
  :global(.cse-sidebar) {
    position: sticky; top: 16px; max-height: calc(100vh - 32px);
    overflow-y: auto; overflow-x: hidden; padding-bottom: 16px;
    -webkit-overflow-scrolling: touch;
  }
  :global(.cse-sidebar::-webkit-scrollbar) { width: 2px; }
  :global(.cse-sidebar::-webkit-scrollbar-thumb) { background: var(--border); border-radius: 2px; }
  :global(.cse-sidebar::-webkit-scrollbar-track) { background: transparent; }
  @media (max-width: 900px) {
    :global(.cse-sidebar) { display: none; }
  }
  :global(.cse-sidebar__header) {
    font-size: 0.65rem; font-weight: 700; text-transform: uppercase;
    letter-spacing: 0.08em; color: var(--dim); padding-bottom: 8px;
    margin-bottom: 4px; border-bottom: 1px solid var(--border);
  }
  :global(.cse-sidebar__entries) { display: flex; flex-direction: column; gap: 2px; }
  :global(.cse-sidebar__group) { margin-bottom: 2px; }
  :global(.cse-sidebar__section) {
    display: block; width: 100%; text-align: left; padding: 5px 8px;
    border: none; border-radius: 4px; background: transparent; cursor: pointer;
    font-family: inherit; font-size: 0.72rem; font-weight: 600;
    color: var(--text); line-height: 1.35; transition: color 0.1s, background 0.1s;
  }
  :global(.cse-sidebar__section:hover) { background: var(--accent-subtle); color: var(--accent); }
  :global(.cse-sidebar__section--active) { color: var(--accent); background: var(--accent-subtle); }
  :global(.cse-sidebar__subs) {
    padding-left: 12px; margin-left: 6px; border-left: 1px solid var(--border); margin-bottom: 6px;
  }
  :global(.cse-sidebar__sub) {
    display: block; width: 100%; text-align: left; padding: 3px 6px;
    border: none; border-radius: 2px; background: transparent; cursor: pointer;
    font-family: inherit; font-size: 0.62rem; color: var(--dim);
    line-height: 1.35; transition: color 0.1s;
  }
  :global(.cse-sidebar__sub:hover) { color: var(--text); }
  :global(.cse-sidebar__sub--active) { color: var(--accent); font-weight: 600; }
  :global(.cse-sidebar__back) {
    margin-top: 12px; padding-top: 10px; border-top: 1px solid var(--border);
  }
  :global(.cse-sidebar__back a) { font-size: 0.65rem; color: var(--dim); text-decoration: none; }
  :global(.cse-sidebar__back a:hover) { color: var(--accent); text-decoration: underline; }
  :global(.cse-content) { min-width: 0; max-width: 780px; }
  :global(.cse-intro) {
    margin-bottom: var(--space-xl); padding: var(--space-md) var(--space-lg);
    background: var(--accent-subtle); border-radius: 8px; border-left: 4px solid var(--accent);
  }
  :global(.cse-section) { margin-bottom: var(--space-2xl); scroll-margin-top: 24px; }
  :global(.cse-section__label) {
    font-size: 0.62rem; font-weight: 600; text-transform: uppercase;
    letter-spacing: 0.1em; color: var(--accent); margin-bottom: 6px; font-family: var(--font-mono);
  }
  :global(.cse-section__title) {
    font-size: 1.75rem; font-weight: 700; line-height: 1.2; letter-spacing: -0.015em;
    color: var(--text); margin-bottom: var(--space-md); padding-bottom: var(--space-sm);
    border-bottom: 2px solid var(--accent);
  }
  @media (max-width: 600px) {
    :global(.cse-section__title) { font-size: 1.35rem; }
  }
  :global(.cse-section__intro) { margin-bottom: var(--space-lg); }
  :global(.cse-section__arguments) { display: flex; flex-direction: column; gap: var(--space-lg); }
  :global(.cse-argument) {
    position: relative; padding: var(--space-md) var(--space-lg);
    background: var(--bg-elevated); border: 1px solid var(--border); border-radius: 8px;
    scroll-margin-top: 24px; transition: border-color 0.2s, box-shadow 0.2s;
  }
  :global(.cse-argument:hover) { border-color: var(--border-strong); box-shadow: var(--shadow-sm); }
  :global(.cse-argument__number) {
    position: absolute; top: var(--space-md); right: var(--space-md);
    font-size: 0.65rem; font-family: var(--font-mono); font-weight: 600; color: var(--dim); opacity: 0.5;
  }
  :global(.cse-argument__title) {
    font-size: 1.1rem; font-weight: 700; line-height: 1.35; color: var(--accent);
    margin-bottom: var(--space-md); padding-right: 2.5rem; padding-bottom: var(--space-sm);
    border-bottom: 1px solid var(--border-subtle);
  }
  @media (max-width: 600px) {
    :global(.cse-argument__title) { font-size: 1rem; }
  }
  :global(.cse-argument__body) { font-size: var(--font-size-base); line-height: 1.7; }
  :global(.cse-back-to-top) {
    position: fixed; bottom: 76px; right: 20px; z-index: 998;
    width: 36px; height: 36px; border-radius: 50%; background: var(--bg);
    color: var(--dim); border: 1px solid var(--border); cursor: pointer; font-size: 1rem;
    display: flex; align-items: center; justify-content: center;
    box-shadow: 0 1px 4px rgba(0,0,0,0.08); transition: color 0.1s, border-color 0.1s;
  }
  :global(.cse-back-to-top:hover) { color: var(--accent); border-color: var(--accent); }
  :global(.cse-mobile-toggle) {
    display: none; position: fixed; bottom: 20px; right: 20px; z-index: 999;
    width: 48px; height: 48px; border-radius: 50%; background: var(--accent);
    color: white; border: none; cursor: pointer; box-shadow: var(--shadow-lg);
    align-items: center; justify-content: center; flex-direction: column; gap: 1px;
    transition: transform var(--transition-fast);
  }
  :global(.cse-mobile-toggle:active) { transform: scale(0.92); }
  :global(.cse-mobile-pct) { font-size: 0.5rem; font-weight: 600; letter-spacing: 0.02em; opacity: 0.8; }
  @media (max-width: 900px) { :global(.cse-mobile-toggle) { display: flex; } }
  @media (min-width: 901px) { :global(.cse-mobile-toggle) { display: none !important; } }
  :global(.cse-mobile-overlay) {
    position: fixed; inset: 0; z-index: 1000; background: rgba(0, 0, 0, 0.5);
    display: flex; align-items: flex-end; justify-content: center;
  }
  :global(.cse-mobile-toc) {
    background: var(--bg); border-radius: 16px 16px 0 0; width: 100%;
    max-height: 80vh; overflow-y: auto;
    padding: var(--space-lg) var(--space-md) var(--space-xl); -webkit-overflow-scrolling: touch;
  }
  :global(.cse-mobile-toc__header) {
    display: flex; justify-content: space-between; align-items: center;
    margin-bottom: var(--space-md); padding-bottom: var(--space-sm);
    border-bottom: 1px solid var(--border); font-size: 0.9rem; font-weight: 700; color: var(--text);
  }
  :global(.cse-mobile-toc__close) { background: none; border: none; color: var(--dim); cursor: pointer; padding: 4px; }
  :global(.cse-mobile-toc__entries) { display: flex; flex-direction: column; gap: 2px; }
  :global(.cse-mobile-toc__group) { margin-bottom: 8px; }
  :global(.cse-mobile-toc__section) {
    display: block; width: 100%; text-align: left; padding: 8px 10px;
    border: none; border-radius: 6px; background: transparent; cursor: pointer;
    font-family: inherit; font-size: 0.95rem; font-weight: 700; color: var(--text); line-height: 1.35;
  }
  :global(.cse-mobile-toc__section--active) { color: var(--accent); background: var(--accent-subtle); }
  :global(.cse-mobile-toc__sub) {
    display: block; width: 100%; text-align: left; padding: 5px 10px 5px 20px;
    border: none; background: transparent; cursor: pointer;
    font-family: inherit; font-size: 0.82rem; color: var(--dim); line-height: 1.4;
  }
  :global(.cse-mobile-toc__sub--active) { color: var(--accent); font-weight: 600; }

  /* Cheat sheet prose overrides */
  .cheat-sheet-page :global(.prose img) { max-width: 100%; height: auto; margin: var(--space-lg) 0; }
  .cheat-sheet-page :global(.prose figure) { margin: var(--space-lg) 0; }
  .cheat-sheet-page :global(.prose figcaption) { font-size: 0.8125rem; color: var(--dim); text-align: center; margin-top: var(--space-sm); }
  .cheat-sheet-page :global(.prose blockquote) {
    border-left: 3px solid var(--accent); padding-left: var(--space-md);
    margin: var(--space-lg) 0; color: var(--dim); font-style: italic;
  }
  .cheat-sheet-page :global(.prose a) {
    color: var(--accent); text-decoration: underline; text-underline-offset: var(--underline-offset);
  }
  .cheat-sheet-page :global(.prose h1),
  .cheat-sheet-page :global(.prose h2),
  .cheat-sheet-page :global(.prose h3) { scroll-margin-top: 20px; color: var(--text); }
  .cheat-sheet-page :global(.prose h3) {
    font-size: 1.05rem; font-weight: 600; line-height: 1.35; margin-top: 1.75rem; margin-bottom: 0.5rem;
  }
  .cheat-sheet-page :global(.prose p) { margin-bottom: var(--space-md); }
  .cheat-sheet-page :global(.prose ul),
  .cheat-sheet-page :global(.prose ol) { margin-bottom: var(--space-md); padding-left: var(--space-lg); }
  .cheat-sheet-page :global(.prose li) { margin-bottom: var(--space-sm); }
  .cheat-sheet-page :global(.prose .image-link),
  .cheat-sheet-page :global(.prose .image2) { display: block; margin: var(--space-lg) 0; }
  .cheat-sheet-page :global(.prose .image-link img),
  .cheat-sheet-page :global(.prose .image2 img) { max-width: 100%; height: auto; }
  .cheat-sheet-page :global(.prose .captioned-image-container) { margin: var(--space-lg) 0; }
  .cheat-sheet-page :global(.prose .image-caption) { font-size: 0.8125rem; color: var(--dim); text-align: center; margin-top: var(--space-sm); }
  .cheat-sheet-page :global(.prose .subscription-widget-wrap),
  .cheat-sheet-page :global(.prose .subscribe-widget),
  .cheat-sheet-page :global(.prose .subscription-widget),
  .cheat-sheet-page :global(.prose .image-link-expand),
  .cheat-sheet-page :global(.prose .icon-container) { display: none !important; }
  .cheat-sheet-page :global(.prose .footnote-anchor) { font-size: 0.7em; vertical-align: super; line-height: 0; text-decoration: none; color: var(--accent); }
  .cheat-sheet-page :global(.prose .footnote-anchor:hover) { text-decoration: underline; }
  .cheat-sheet-page :global(.prose .footnotes),
  .cheat-sheet-page :global(.prose .footnote) {
    font-size: 0.8125rem; color: var(--dim); margin-top: var(--space-xl);
    padding-top: var(--space-md); border-top: 1px solid var(--border);
  }
  .cheat-sheet-page :global(.prose .footnote .footnote-number) { font-size: 0.85em; margin-right: var(--space-sm); color: var(--accent); text-decoration: none; }
  .cheat-sheet-page :global(.prose pre) { background: var(--border); padding: var(--space-md); border-radius: 4px; overflow-x: auto; font-family: var(--font-mono); font-size: 0.8125rem; }
  .cheat-sheet-page :global(.prose code) { font-family: var(--font-mono); font-size: 0.8125rem; background: var(--border); padding: 0.1em 0.3em; border-radius: 2px; }
  .cheat-sheet-page :global(.prose pre code) { background: none; padding: 0; }
</style>
