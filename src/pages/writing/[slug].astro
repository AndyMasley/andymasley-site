---
import Base from '@/layouts/Base.astro';
import { fetchSubstackPosts, fetchPostContent, processPostContent, type SubstackPost } from '@/lib/substack';
import { fetchEAForumPosts, fetchEAForumPostContent, processEAForumContent, type EAForumPost } from '@/lib/eaforum';
import { metaPosts, getMetaPostSlugs } from '@/lib/meta-posts';
import { CheatSheetExplorer, parseHtmlIntoSections } from '@/components/CheatSheetExplorer';
import { ArticleTOC } from '@/components/ArticleTOC';
import { extractHeadingsFromHtml, type TOCHeading } from '@/lib/extract-headings';
import { mapWithConcurrency } from '@/lib/map-with-concurrency';
import { getOptionalCollection } from '@/lib/optional-collection';
import { formatPostDate as formatDate, formatDateOnly } from '@/lib/format-date';
import { latestCorrection } from '@/data/corrections';
import { getSeriesEntry, getSeriesSiblings } from '@/data/series';
import { getCitedBy } from '@/lib/cited-in';
import { getCategorySlug, getCategoryDisplayName } from '@/lib/category-slug';
import { coverSocialCard } from '@/lib/cover-image';

// Union type for all post sources
type PostSource = 'local' | 'substack' | 'eaforum';

interface LocalPost {
  source: 'local';
  slug: string;
  title: string;
  date: Date;
  description: string;
  tags: string[];
  body: string;
  render: () => Promise<{ Content: any; headings: any[] }>;
}

interface ExternalPost {
  source: 'substack' | 'eaforum';
  slug: string;
  title: string;
  date: Date;
  description: string;
  url: string;
  htmlContent: string;
  sourceName: string;
  category?: string;
  cover_image?: string;
}

type Post = LocalPost | ExternalPost;

interface AdjacentPost {
  slug: string;
  title: string;
  date: Date;
}

export async function getStaticPaths() {
  const paths: Array<{ params: { slug: string }; props: { post: Post } }> = [];

  // 1. Get post lists from all sources
  const [localPosts, substackPosts, eaForumPosts] = await Promise.all([
    getOptionalCollection('writing'),
    fetchSubstackPosts(),
    fetchEAForumPosts()
  ]);

  const existingSlugs = new Set<string>();

  for (const post of localPosts) {
    paths.push({
      params: { slug: post.slug },
      props: {
        post: {
          source: 'local' as const,
          slug: post.slug,
          title: post.data.title,
          date: post.data.date,
          description: post.data.description || '',
          tags: post.data.tags || [],
          body: post.body,
          render: () => post.render(),
        }
      }
    });
    existingSlugs.add(post.slug);
  }

  // 2. Get Substack posts
  const metaSlugSet = new Set(getMetaPostSlugs());
  const substackCandidates = substackPosts.filter(
    post => !existingSlugs.has(post.slug)
  );
  const substackPaths = await mapWithConcurrency(substackCandidates, async post => {
    const rawContent = await fetchPostContent(post.slug);
    const htmlContent = processPostContent(rawContent, post.slug);

    return {
      params: { slug: post.slug },
      props: {
        post: {
          source: 'substack' as const,
          slug: post.slug,
          title: post.title,
          date: post.date,
          description: post.description,
          url: post.url,
          htmlContent,
          sourceName: 'Substack',
          category: post.category,
          cover_image: post.cover_image,
        }
      }
    };
  });
  paths.push(...substackPaths);
  substackCandidates.forEach(post => existingSlugs.add(post.slug));

  const missingMetaPosts = metaPosts.filter(post => !existingSlugs.has(post.slug));
  const missingMetaPaths = await mapWithConcurrency(missingMetaPosts, async post => {
    const rawContent = await fetchPostContent(post.slug);
    const htmlContent = processPostContent(rawContent, post.slug);

    return {
      params: { slug: post.slug },
      props: {
        post: {
          source: 'substack' as const,
          slug: post.slug,
          title: post.title,
          date: new Date(0),
          description: '',
          url: post.url,
          htmlContent,
          sourceName: 'Substack',
        }
      }
    };
  });
  paths.push(...missingMetaPaths);
  missingMetaPosts.forEach(post => existingSlugs.add(post.slug));

  // 3. Get EA Forum posts
  const eaForumCandidates = eaForumPosts.filter(post => !existingSlugs.has(post.slug));
  const eaForumPaths = await mapWithConcurrency(eaForumCandidates, async post => {
    const rawContent = await fetchEAForumPostContent(post.postId);
    const htmlContent = processEAForumContent(rawContent, post.slug);

    return {
      params: { slug: post.slug },
      props: {
        post: {
          source: 'eaforum' as const,
          slug: post.slug,
          title: post.title,
          date: post.date,
          description: '',
          url: post.url,
          htmlContent,
          sourceName: 'EA Forum',
          category: 'EA Forum',
        }
      }
    };
  });
  paths.push(...eaForumPaths);

  const sortedPaths = [...paths].sort(
    (a, b) => b.props.post.date.getTime() - a.props.post.date.getTime()
  );

  const navigablePaths = sortedPaths.filter(path => !metaSlugSet.has(path.props.post.slug));
  const postBySlug = new Map(sortedPaths.map(path => [path.props.post.slug, path.props.post]));
  const toAdjacent = (slug: string): AdjacentPost | undefined => {
    const p = postBySlug.get(slug);
    return p ? { slug: p.slug, title: p.title, date: p.date } : undefined;
  };

  return sortedPaths.map((path) => {
    const slug = path.props.post.slug;
    if (metaSlugSet.has(slug)) {
      return { ...path, props: { ...path.props, newerPost: undefined, olderPost: undefined } };
    }
    const navIndex = navigablePaths.findIndex(item => item.props.post.slug === slug);
    let newerPost = navIndex > 0
      ? toAdjacent(navigablePaths[navIndex - 1].props.post.slug)
      : undefined;
    let olderPost = navIndex >= 0 && navIndex < navigablePaths.length - 1
      ? toAdjacent(navigablePaths[navIndex + 1].props.post.slug)
      : undefined;

    // Series-aware neighbors: within a real sequence, older/newer walk the
    // parts before falling back to site-wide chronology — the chronological
    // neighbor of part 2 of the fitness series should be part 1, not
    // whatever else was published that week.
    const seriesEntry = getSeriesEntry(slug);
    if (seriesEntry) {
      const siblings = getSeriesSiblings(slug);
      const prevPart = siblings.filter(s => s.part < seriesEntry.part).sort((a, b) => b.part - a.part)[0];
      const nextPart = siblings.filter(s => s.part > seriesEntry.part).sort((a, b) => a.part - b.part)[0];
      if (prevPart) olderPost = toAdjacent(prevPart.slug) ?? olderPost;
      if (nextPart) newerPost = toAdjacent(nextPart.slug) ?? newerPost;
    }

    return { ...path, props: { ...path.props, newerPost, olderPost } };
  });
}

interface Props {
  post: Post;
  newerPost?: AdjacentPost;
  olderPost?: AdjacentPost;
}

const { post, newerPost, olderPost } = Astro.props;
const isMetaPost = new Set(getMetaPostSlugs()).has(post.slug);
// Post pages self-canonicalize (Base falls back to the current andymasley.com
// path). Substack declares its own canonical on the originals, so both copies
// deliberately claim themselves — don't "fix" this by pointing ours at Substack.
const permalink = new URL(`/writing/${post.slug}/`, Astro.site ?? 'https://andymasley.com').href;
// The social card is the post's own Substack cover art. The generated
// /og/<slug>.png title card (og-card.ts) stays as the fallback for a post
// with no cover, and remains the alt text in that case.
const subscribeUrl = 'https://andymasley.substack.com/subscribe';
const coverImage = post.source !== 'local' ? post.cover_image : undefined;
const socialCard = coverImage ? coverSocialCard(coverImage, 1200) : null;
const ogImage = socialCard ? socialCard.url : `/og/${post.slug}.png?v=2`;
const ogImageWidth = socialCard ? socialCard.width : 1200;
const ogImageHeight = socialCard ? socialCard.height : 630;
const ogImageAlt = socialCard
  ? `Cover art for "${post.title}"`
  : `Title card reading "${post.title}" on a plain white background, andymasley.com`;


// Estimate reading time
function getReadingTime(content: string): number {
  const text = content.replace(/<[^>]*>/g, ''); // Strip HTML
  const wordCount = text.split(/\s+/).length;
  return Math.ceil(wordCount / 200);
}

// Get content and metadata based on source
let Content: any = null;
let readingTime = 0;
let tocHeadings: TOCHeading[] = [];
let displayHtml = '';

// The guide/hub posts are directories, not essays: Substack's editor sets
// every linked title in bold (bold + blue is double emphasis, and sixty of
// them make a wall) and fakes subgroup headings as bold-only paragraphs.
// Recompose both at build time so the CSS can set the guide like a
// well-kept index: plain blue titles, spaced-caps subgroup labels.
function refineGuideHtml(html: string): string {
  let out = html;
  // A paragraph that is exactly one bold run of plain text is a subgroup
  // label, not prose.
  out = out.replace(/<p><strong>([^<]{2,48}?)\s*<\/strong><\/p>/g, '<p class="guide-sublabel">$1</p>');
  // Unbold linked titles and bare "Part N:" prefixes beside them.
  out = out.replace(/<strong>\s*(<a[^>]*>[\s\S]*?<\/a>)(\s*)<\/strong>/g, '$1$2');
  out = out.replace(/<strong>\s*(Part \d+:?)(\s*)<\/strong>/g, '$1$2');
  // An item's annotation becomes a quiet note on its own line under the
  // title, instead of running into the link text.
  out = out.replace(
    /(<li><p>)((?:Part \d+:?\s*)?<a[^>]*>[\s\S]*?<\/a>)([\s\S]*?)(<\/p>)/g,
    (m: string, open: string, link: string, tail: string, close: string) => {
      const note = tail.replace(/^[\s—–-]+/, '');
      if (!note.replace(/<[^>]+>/g, '').trim()) return m;
      return `${open}${link}<span class="guide-note">${note}</span>${close}`;
    }
  );
  // House sentence case on the one Substack-era heading that breaks it.
  out = out.replace(/Data Centers(?=\s*(?:<a\b|<\/h[1-6]>))/, 'Data centers');
  // Curated additions the Substack original doesn't carry yet (Andy's
  // call, Aug 2026). Idempotent: skipped once the post itself links it.
  out = out.replace(
    /(<h[1-6][^>]*id="environmental-ethics"[\s\S]*?<ul>)([\s\S]*?)(<\/ul>)/,
    (m: string, open: string, items: string, close: string) =>
      items.includes('data-centers-and-low-social-trust')
        ? m
        : `${open}${items}<li><p><a href="/writing/data-centers-and-low-social-trust">Data centers and low social trust</a></p></li>${close}`
  );
  return out;
}

if (post.source === 'local') {
  const rendered = await post.render();
  Content = rendered.Content;
  readingTime = Math.ceil(post.body.split(/\s+/).length / 200);
  // Map Astro headings to TOC format
  tocHeadings = (rendered.headings || [])
    .filter((h: any) => h.depth === 2 || h.depth === 3)
    .map((h: any) => ({ id: h.slug, text: h.text, level: h.depth === 2 ? 'section' as const : 'sub' as const }));
} else if (isMetaPost) {
  // Guides skip the essay chrome entirely — no TOC rail, no reading time.
  displayHtml = refineGuideHtml(post.htmlContent);
} else {
  readingTime = getReadingTime(post.htmlContent);
  const extracted = extractHeadingsFromHtml(post.htmlContent);
  tocHeadings = extracted.headings;
  displayHtml = extracted.cleanedHtml;
}

// Build-time guard: the explorer needs parseable sections. If the pipeline
// ever changes shape again, warn loudly and fall back to the standard essay
// layout for this post rather than shipping an empty Navigate rail — one
// drifted post must never block deploys of the other ninety.
let isCheatSheet = post.slug === 'a-cheat-sheet-for-conversations-about';
if (isCheatSheet && post.source !== 'local') {
  const parsedCheat = parseHtmlIntoSections(post.htmlContent);
  if (parsedCheat.sections.length < 2) {
    console.warn(
      `[cheatsheet] ${post.slug}: only ${parsedCheat.sections.length} section(s) parsed from processed HTML — falling back to the standard essay layout`
    );
    isCheatSheet = false;
  }
}
const wordCount = post.source === 'local'
  ? post.body.split(/\s+/).length
  : (post.htmlContent.replace(/<[^>]*>/g, '').split(/\s+/).length);
const standfirst = post.description?.trim() || '';

// The ledger's stamp: a corrected essay names its correction in the meta
// row and carries dateModified, so rigor is visible where readers form
// judgments. Data is hand-curated in src/data/corrections.ts.
const correction = latestCorrection(post.slug);
const correctionLabel = correction
  ? `${correction.kind === 'correction' ? 'corrected' : 'updated'} ${formatDateOnly(correction.date)}`
  : '';

// Series line + shelf kicker. The kicker names the post's topic and links
// the archive's shareable filter URL; series posts carry a part-title line
// under the standfirst, set like a book's.
const seriesEntry = getSeriesEntry(post.slug);
const category = post.source !== 'local' ? post.category : undefined;
const categorySlug = category ? getCategorySlug(category) : '';
const categoryName = category ? getCategoryDisplayName(category) : '';

// Cited in: the internal link graph, inverted at build time. Capped at
// five, most recent first, omitted when empty.
const citedBy = isMetaPost ? [] : (await getCitedBy(post.slug)).slice(0, 5);

const articleStructuredData: Record<string, unknown> = {
  "@context": "https://schema.org",
  "@type": "Article",
  "headline": post.title,
  "author": {
    "@type": "Person",
    "name": "Andy Masley",
    "url": "https://andymasley.com"
  },
  "datePublished": post.date.toISOString(),
  "description": post.description || '',
  "wordCount": wordCount,
  "url": `https://andymasley.com/writing/${post.slug}`,
  "publisher": {
    "@type": "Person",
    "name": "Andy Masley"
  },
  "mainEntityOfPage": {
    "@type": "WebPage",
    "@id": `https://andymasley.com/writing/${post.slug}`
  }
};
if (correction) {
  articleStructuredData.dateModified = new Date(`${correction.date}T12:00:00Z`).toISOString();
}
---

{isCheatSheet && post.source !== 'local' ? (
<Base
  title={post.title}
  description="Every environmental objection to using ChatGPT, answered with data and sources."
  ogType="article"
  ogImage={ogImage}
  ogImageAlt={ogImageAlt}
  ogImageWidth={ogImageWidth}
  ogImageHeight={ogImageHeight}
  structuredData={articleStructuredData}
>
  <meta slot="head" property="article:published_time" content={post.date.toISOString()} />
  <meta slot="head" property="article:author" content="Andy Masley" />
  <div class="cheat-sheet-page">
    <header class="article-header">
      <p class="article-print-attribution" aria-hidden="true">Andy Masley · andymasley.com/writing/{post.slug}</p>
      <h1 class="article-header__title">{post.title}</h1>
      <div class="article-header__meta">
        <span><time datetime={post.date.toISOString()}>{formatDate(post.date)}</time></span>
        <span>{readingTime} min read</span>
        <span>{Math.round(wordCount / 1000)}k words</span>
      </div>
    </header>

    <CheatSheetExplorer client:load html={post.htmlContent} />

    <div class="divider article-endmark" aria-hidden="true"><div class="divider-diamond"></div></div>
    <footer class="article-footer">
      <p class="article-colophon">
        <a href={post.url} target="_blank" rel="noopener">Discussion on {post.sourceName}</a>
        {' '}· <a href={subscribeUrl} target="_blank" rel="noopener">Subscribe for new essays</a>
        {' '}· spot an error? <a href="/writing/errors-and-updates">there’s a $300 bounty</a>.
      </p>
      <p class="article-backlink">
        <a href="/writing">&larr; back to writing</a>
      </p>
    </footer>
  </div>
</Base>
) : (
<Base
  title={post.title}
  description={post.description}
  activeNav="/writing"
  ogType="article"
  ogImage={ogImage}
  ogImageAlt={ogImageAlt}
  ogImageWidth={ogImageWidth}
  ogImageHeight={ogImageHeight}
  structuredData={articleStructuredData}
>
  <meta slot="head" property="article:published_time" content={post.date.toISOString()} />
  <meta slot="head" property="article:author" content="Andy Masley" />
  {correction && (
    <meta slot="head" property="article:modified_time" content={new Date(`${correction.date}T12:00:00Z`).toISOString()} />
  )}
  <div class={isMetaPost ? 'article-page guide-page' : 'article-page article-page--wide'}>
    <div class={isMetaPost ? 'article-plain' : 'article-grid article-grid--wide'}>
      {!isMetaPost && (
        <aside class="article-grid__toc">
          <ArticleTOC
            client:load
            headings={tocHeadings}
            meta={{
              date: formatDate(post.date),
              readingTime: `${readingTime} min read`,
              length: `${Math.round(wordCount / 100) / 10}k words`,
              sourceName: post.source !== 'local' ? post.sourceName : undefined,
              sourceUrl: post.source !== 'local' ? post.url : undefined,
            }}
          />
        </aside>
      )}
      <div class={isMetaPost ? 'article-main h-entry' : 'article-main article-grid__main h-entry'}>
        <a class="u-url" href={permalink} hidden aria-hidden="true"></a>
        <header class="article-header">
          <p class="article-print-attribution" aria-hidden="true">Andy Masley · andymasley.com/writing/{post.slug}</p>
          <div class="article-header__meta">
            {!isMetaPost && (
              <span class="article-header__meta-item">
                <time class="dt-published" datetime={post.date.toISOString()}>{formatDate(post.date)}</time>
              </span>
            )}
            {isMetaPost ? (
              <span class="article-header__meta-item">Guide</span>
            ) : (
              <span class="article-header__meta-item">{readingTime} min read</span>
            )}
            {post.source !== 'local' && !isMetaPost && (
              <span class="article-header__meta-item">
                Originally published on <a href={post.url} target="_blank" rel="noopener">{post.sourceName}</a>
              </span>
            )}
            {categoryName && !isMetaPost && (
              <span class="article-header__meta-item">
                <a class="article-header__kicker" href={`/writing?topic=${categorySlug}`}>{categoryName}</a>
              </span>
            )}
            {correction && (
              <span class="article-header__meta-item">
                <a class="article-header__corrected" href={`/writing/errors-and-updates#${correction.anchor}`}>{correctionLabel}</a>
              </span>
            )}
          </div>
          <h1 class="article-header__title p-name">{post.title}</h1>
          {standfirst && <p class="article-header__standfirst">{standfirst}</p>}
          {seriesEntry && (
            <p class="article-header__series">
              Part {seriesEntry.part} of{' '}
              {seriesEntry.hubSlug
                ? <a href={`/writing/${seriesEntry.hubSlug}`}>{seriesEntry.series}</a>
                : seriesEntry.series}
            </p>
          )}
        </header>

        <article class="article-body">
          <div class="article-prose prose e-content">
            {post.source === 'local' ? (
              <Content />
            ) : (
              <Fragment set:html={displayHtml || post.htmlContent} />
            )}
          </div>
          {/* End matter, under the permanent ceiling stated in CLAUDE.md:
              diamond, notes (inside the prose above), one colophon line,
              cited-in (≤5, only when non-empty), plain older/newer, one
              back-link. Nothing else, ever. */}
          <div class="divider article-endmark" aria-hidden="true"><div class="divider-diamond"></div></div>
          <footer class="article-footer">
            <p class="article-colophon">
              {post.source !== 'local' && (
                <><a href={post.url} target="_blank" rel="noopener">Discussion on {post.sourceName}</a> · </>
              )}
              <a href={subscribeUrl} target="_blank" rel="noopener">Subscribe for new essays</a>
              {' '}· spot an error? <a href="/writing/errors-and-updates">there’s a $300 bounty</a>.
            </p>
            {citedBy.length > 0 && (
              <div class="article-citedin">
                <p class="article-citedin__label">Cited in</p>
                <ul class="article-citedin__list">
                  {citedBy.map(citer => (
                    <li>
                      <a href={`/writing/${citer.slug}`}>{citer.title}</a>
                      <span class="article-citedin__year">{citer.date.getFullYear()}</span>
                    </li>
                  ))}
                </ul>
              </div>
            )}
            {(newerPost || olderPost) && (
              <nav class="article-adjacent" aria-label="More writing">
                {olderPost && (
                  <a class="article-adjacent__item article-adjacent__item--older" href={`/writing/${olderPost.slug}`}>
                    <span class="article-adjacent__label">&larr; Older</span>
                    <span class="article-adjacent__title">{olderPost.title}</span>
                  </a>
                )}
                {newerPost && (
                  <a class="article-adjacent__item article-adjacent__item--newer" href={`/writing/${newerPost.slug}`}>
                    <span class="article-adjacent__label">Newer &rarr;</span>
                    <span class="article-adjacent__title">{newerPost.title}</span>
                  </a>
                )}
              </nav>
            )}
            <p class="article-backlink">
              <a href="/writing">&larr; back to writing</a>
            </p>
          </footer>
        </article>
      </div>
    </div>
  </div>
</Base>
)}

<style>
  .article-page {
    width: 100%;
  }

  .article-page--standard {
    max-width: 720px;
    margin: 0 auto;
  }

  /* One measure, one axis: the essay column IS the standard centered
     640px column. At >=1280px the TOC hangs into the left margin — the
     explicit width plus negative margin keeps body text aligned with the
     header ink by construction, not by shell arithmetic. */
  @media (min-width: 1280px) {
    .article-grid--wide {
      display: grid;
      grid-template-columns: 220px var(--measure);
      column-gap: 3rem;
      width: calc(220px + 3rem + var(--measure));
      margin-left: calc(-220px - 3rem);
      align-items: start;
    }

    .article-grid__main {
      grid-column: 2;
    }

    .article-grid__toc {
      grid-column: 1;
      min-width: 0;
      align-self: start;
      position: sticky;
      top: 1.5rem;
    }
  }

  .article-main {
    min-width: 0;
  }

  .article-body {
    min-width: 0;
  }

  .article-header {
    margin-bottom: clamp(2.5rem, 5vw, 4rem);
    padding-bottom: clamp(1.5rem, 3vw, 2rem);
    border-bottom: 1px solid var(--border);
  }

  .article-header__title {
    font-family: var(--font-editorial);
    font-size: var(--text-display);
    font-weight: 700;
    line-height: var(--leading-tight);
    letter-spacing: -0.01em;
    text-wrap: balance;
    margin: 0;
  }

  .article-header__meta {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem 0.875rem;
    margin-bottom: 1rem;
    font-size: var(--text-xs);
    font-family: var(--font-body);
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--dim);
    font-variant-numeric: tabular-nums;
  }

  .article-header__meta-item {
    display: inline-flex;
    align-items: center;
    gap: 0.875rem;
    line-height: 1.4;
  }

  /* Separator rides ::before on the FOLLOWING item, so when the row wraps
     the break lands between items and no line ends on a dangling '·'. */
  .article-header__meta-item:not(:first-child)::before {
    content: '·';
    color: var(--border-strong);
  }

  .article-header__meta-item a {
    color: inherit;
    color: var(--accent);
  }

  .article-header__standfirst {
    margin: 1.5rem 0 0;
    font-family: var(--font-editorial);
    font-style: italic;
    font-size: var(--text-lg);
    line-height: var(--leading-note);
    color: var(--text-secondary);
    text-wrap: balance;
  }

  /* Series part-title, set like a book's running line. */
  .article-header__series {
    margin: 0.9rem 0 0;
    font-family: var(--font-body);
    font-size: var(--text-xs);
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--dim);
  }

  .article-header__series a {
    color: var(--accent);
  }

  /* The guide (the flagship hub post) is a directory set like a well-kept
     index, not an essay: plain centered measure, section headings at the
     section scale, spaced-caps subgroup labels, comfortable list rhythm.
     The unbolding itself happens at build (refineGuideHtml). */
  /* The theme sections arrive as h4s (the heading normalizer shifts the
     guide's ladder down one level); set them at the section scale. */
  .guide-page .article-prose :global(h3),
  .guide-page .article-prose :global(h4) {
    font-size: var(--text-xl);
    line-height: var(--leading-heading);
    margin-top: var(--space-2xl);
    margin-bottom: var(--space-md);
  }

  .guide-page .article-prose :global(.guide-sublabel) {
    margin: var(--space-lg) 0 var(--space-sm);
    font-family: var(--font-body);
    font-size: var(--text-xs);
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--dim);
  }

  .guide-page .article-prose :global(ul) {
    margin-bottom: var(--space-lg);
  }

  .guide-page .article-prose :global(li) {
    margin-bottom: 0.6rem;
  }

  .guide-page .article-prose :global(li > p) {
    margin-bottom: 0;
  }

  .guide-page .article-prose :global(li > ul) {
    margin: 0.45rem 0 0;
  }

  .guide-page .article-prose :global(.guide-note) {
    display: block;
    font-size: var(--text-base);
    line-height: var(--leading-ui);
    color: var(--text-secondary);
    margin-top: 0.1rem;
  }

  /* The end matter is typography, not chrome: a diamond, one colophon
     line, a bare cited-in list, plain older/newer lines, one back-link.
     The ceiling lives in CLAUDE.md; nothing here grows a border or box. */
  .article-endmark {
    margin-top: clamp(3rem, 6vw, 4.5rem);
  }

  .article-footer {
    margin-top: var(--space-xl);
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .article-colophon {
    margin: 0;
    font-size: var(--text-sm);
    line-height: var(--leading-ui);
    color: var(--text-secondary);
  }

  .article-citedin__label {
    margin: 0 0 0.4rem;
    font-family: var(--font-body);
    font-size: var(--text-xs);
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--dim);
  }

  .article-citedin__list {
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .article-citedin__list li {
    margin: 0 0 0.3rem;
    font-size: var(--text-sm);
    line-height: var(--leading-ui);
  }

  .article-citedin__year {
    margin-left: 0.5rem;
    color: var(--dim);
    font-size: var(--text-xs);
    font-variant-numeric: tabular-nums;
  }

  /* Older left, newer right. Still typography, not chrome: no border, no
     box. The label carries the direction so the title can be a plain
     title, and the title takes the ink-with-hairline treatment the Recent
     list uses rather than full-width blue. */
  .article-adjacent {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-lg);
    align-items: start;
  }

  /* One neighbour only: it keeps its own side of the grid. */
  .article-adjacent__item--newer:only-child {
    grid-column: 2;
  }

  .article-adjacent__item {
    display: block;
    min-width: 0;
    text-decoration: none;
  }

  .article-adjacent__item--newer {
    text-align: right;
  }

  .article-adjacent__label {
    display: block;
    margin-bottom: 0.2rem;
    font-family: var(--font-label);
    font-size: var(--text-xs);
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--dim);
  }

  .article-adjacent__title {
    font-size: var(--text-sm);
    line-height: var(--leading-note);
    color: var(--text);
    text-decoration: underline;
    text-decoration-thickness: 1px;
    text-underline-offset: 2px;
    text-decoration-color: var(--border-strong);
    /* A long neighbour title stops at two lines instead of running away. */
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .article-adjacent__item:hover .article-adjacent__title {
    color: var(--accent);
    text-decoration-color: var(--accent);
  }

  .article-adjacent__item:hover .article-adjacent__label {
    color: var(--accent);
  }

  @media (max-width: 560px) {
    .article-adjacent {
      grid-template-columns: 1fr;
      gap: var(--space-md);
    }

    .article-adjacent__item--newer,
    .article-adjacent__item--newer:only-child {
      grid-column: 1;
      text-align: left;
    }
  }

  .article-backlink {
    margin: 0;
    font-size: var(--text-sm);
  }

  .article-backlink a {
    color: var(--dim);
    text-decoration: underline;
    text-underline-offset: var(--underline-offset);
  }

  .article-backlink a:hover {
    color: var(--text);
  }

  .article-prose {
    font-family: var(--font-editorial);
    font-size: var(--text-md);
    line-height: var(--leading-prose);
    color: var(--text);
    text-align: left;
    hyphens: none;
  }

  /* Micro-typography: balanced headings, pretty body paragraphs */
  .article-prose :global(h1),
  .article-prose :global(h2),
  .article-prose :global(h3) { text-wrap: balance; }
  .article-prose :global(p) { text-wrap: pretty; }

  /* Section anchors (§): revealed on hover/focus, faint on touch. Scope
     covers the cheat-sheet explorer's injected prose too, so its headings
     stop rendering a permanently visible anchor. */
  .article-prose :global(.h-anchor),
  .cheat-sheet-page :global(.prose .h-anchor) {
    margin-left: 0.4em;
    color: var(--dim);
    text-decoration: none;
    opacity: 0;
  }
  .article-prose :global(h1:hover .h-anchor),
  .article-prose :global(h2:hover .h-anchor),
  .article-prose :global(h3:hover .h-anchor),
  .article-prose :global(h4:hover .h-anchor),
  .article-prose :global(.h-anchor:focus-visible),
  .cheat-sheet-page :global(.prose h2:hover .h-anchor),
  .cheat-sheet-page :global(.prose h3:hover .h-anchor),
  .cheat-sheet-page :global(.prose h4:hover .h-anchor),
  .cheat-sheet-page :global(.prose .h-anchor:focus-visible) { opacity: 1; }
  @media (hover: none) {
    .article-prose :global(.h-anchor),
    .cheat-sheet-page :global(.prose .h-anchor) { opacity: 0.35; }
  }

  /* Scene breaks */
  .article-prose :global(.asterism) {
    text-align: center;
    color: var(--dim);
    font-size: 1.1em;
    margin: 2.25rem 0;
  }

  .article-prose :global(p) {
    margin-bottom: 1.5rem;
  }

  /* Images ride the column's axis: block flow, centered when narrower
     than the measure (honest-dimensions images from the import render at
     their natural width). Wrappers inside a figure give up their margins
     so the caption sits against its image, not 40px below it. */
  .article-prose :global(img) {
    display: block;
    max-width: 100%;
    height: auto;
    margin: 2.25rem auto 0;
    border-radius: 0;
    border: 1px solid var(--border);
    background: transparent;
    box-shadow: none;
  }

  .article-prose :global(figure) {
    margin: 2.5rem 0;
  }

  .article-prose :global(figure img) {
    margin: 0 auto;
  }

  .article-prose :global(figure picture),
  .article-prose :global(figure .image-link),
  .article-prose :global(figure .image2) {
    display: block;
    margin: 0;
  }

  /* Captions: secondary ink, smaller than body, hanging indent so wrapped
     lines tuck under the leading "Source:" label. .image-caption is the
     class Substack puts on its figcaptions — same treatment. */
  .article-prose :global(figcaption),
  .article-prose :global(.image-caption) {
    margin-top: 0.85rem;
    max-width: 38rem;
    font-size: var(--text-sm);
    font-family: var(--font-body);
    line-height: var(--leading-ui);
    color: var(--dim);
    text-align: left;
    padding-left: 1.1em;
    text-indent: -1.1em;
  }

  .article-prose :global(blockquote) {
    margin: 2.25rem 0;
    padding: 0.25rem 0 0.25rem 1.25rem;
    border-left: 1.5px solid var(--border-strong);
    color: var(--text-secondary);
    font-style: normal;
  }

  /* The page-scoped paragraph margin above outranks global.css's
     `blockquote > :last-child` reset, so the left rule kept drawing 24px
     past the quote's last line. Re-assert the reset at page scope:
     nothing hangs below the text. */
  .article-prose :global(blockquote > :last-child) {
    margin-bottom: 0;
  }

  /* Blockquote citations */
  .article-prose :global(blockquote > footer),
  .article-prose :global(blockquote cite) {
    color: var(--dim);
    font-style: normal;
    font-size: 0.9em;
  }
  .article-prose :global(blockquote > footer::before) { content: "\2014\00a0"; }

  /* Tables: ink rules top and bottom, hairline under the header, no zebra */
  .article-prose :global(table) {
    border-collapse: collapse;
    margin: 1.5rem 0;
    border-top: 1px solid var(--text);
    border-bottom: 1px solid var(--text);
    font-variant-numeric: lining-nums tabular-nums;
  }
  .article-prose :global(th) {
    border-bottom: 1px solid var(--border);
    text-align: left;
  }
  .article-prose :global(th),
  .article-prose :global(td) {
    padding: 0.4rem 1rem 0.4rem 0;
    border-left: 0;
    border-right: 0;
  }
  .article-prose :global(tbody tr) { background: none; }
  .article-prose :global(td.num),
  .article-prose :global(th.num) { text-align: right; }

  .article-prose :global(pre) {
    margin: 2rem 0;
    padding: 1rem 1.125rem;
    border: 1px solid var(--border);
    border-radius: 0;
    background: var(--bg-subtle);
    overflow-x: auto;
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    line-height: var(--leading-ui);
    color: var(--text-secondary);
  }

  .article-prose :global(code) {
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    background: var(--bg-subtle);
    padding: 0.14em 0.36em;
    border: 1px solid var(--border);
    border-radius: 0;
  }

  .article-prose :global(pre code) {
    background: none;
    padding: 0;
  }

  /* Visited purple (post prose). Literal colors: :visited ignores var(). */
  .article-prose :global(a:visited) { color: #551a8b; }
  :global([data-theme="dark"]) .article-prose :global(a:visited) { color: #c58af9; }

  /* External links read as ordinary hyperlinks — no ↗ marker (Andy's
     call, Aug 2026). Print still expands their URLs (global.css). */

  .article-prose :global(h1) {
    font-family: var(--font-editorial);
    font-size: var(--text-display);
    font-weight: 700;
    line-height: 1.12;
    letter-spacing: -0.01em;
    color: var(--text);
    margin-top: 4rem;
    margin-bottom: 1rem;
  }

  .article-prose :global(h1),
  .article-prose :global(h2),
  .article-prose :global(h3) {
    scroll-margin-top: 24px;
  }

  .article-prose :global(h1:first-child),
  .article-prose :global(h2:first-child) {
    margin-top: 0;
  }

  .article-prose :global(ul),
  .article-prose :global(ol) {
    margin-bottom: 1.5rem;
    padding-left: 1.35rem;
  }

  /* Top-level lists indent from the text column, at the same step the
     lists pages use (--space-xl). Markers used to hang in the gutter at
     ≥1024px so the column's left edge ran flush; Andy's call, Aug 2026, is
     that a list should read as indented from the prose instead. Nested
     lists keep the tighter 1.35rem above. */
  .article-prose > :global(ul),
  .article-prose > :global(ol) {
    padding-left: var(--space-xl);
  }

  .article-prose :global(li) {
    margin-bottom: 0.45rem;
    padding-left: 0.2rem;
  }

  .article-prose :global(hr) {
    margin: 3rem 0;
    border: none;
    border-top: 1px solid var(--border);
  }

  .article-prose :global(.image-link),
  .article-prose :global(.image2) {
    display: block;
    margin: 2.25rem 0;
  }

  .article-prose :global(.image-link img),
  .article-prose :global(.image2 img) {
    max-width: 100%;
    height: auto;
  }

  .article-prose :global(.captioned-image-container) {
    margin: 2.25rem 0;
  }

  .article-prose :global(.subscription-widget-wrap),
  .article-prose :global(.subscribe-widget),
  .article-prose :global(.subscription-widget),
  .article-prose :global(.image-link-expand),
  .article-prose :global(.icon-container) {
    display: none !important;
  }

  .article-prose :global(sup) {
    font-size: 0.7em;
    line-height: 0;
    vertical-align: baseline;
    position: relative;
    top: -0.4em;
  }

  .article-prose :global(sup a) {
    font-size: inherit;
    text-decoration: none;
    color: var(--accent);
  }

  .article-prose :global(sup a:hover) {
    text-decoration: underline;
  }

  /* Footnote and sidenote styling is global (see global.css: Components:
     Footnotes / Sidenotes) so the homepage bio note renders identically. */

  /* The running-head button is transparent by design (its bar carries the
     page background); the drawer must stay opaque over the scrim. */
  :global([data-theme="dark"]) :global(.article-toc-mobilebar__button) {
    background: transparent;
  }

  :global(.article-toc-shell) {
    min-width: 0;
  }

  :global(.article-toc) {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    padding-right: 1rem;
    font-variant-numeric: tabular-nums;
  }

  :global(.article-toc__header) {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    gap: 1rem;
    padding-bottom: 0.85rem;
    border-bottom: 1px solid var(--border);
  }

  :global(.article-toc__eyebrow),
  :global(.article-toc__footer a),
  :global(.article-toc-drawer__eyebrow) {
    font-size: var(--text-xs);
    font-family: var(--font-body);
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--dim);
  }

  :global(.article-toc__entries) {
    display: flex;
    flex-direction: column;
    gap: 0.85rem;
  }







  :global(.article-toc__group) {
    display: flex;
    flex-direction: column;
    gap: 0.45rem;
  }

  :global(.article-toc__section),
  :global(.article-toc__sub) {
    display: block;
    display: -webkit-box;
    -webkit-box-orient: vertical;
    -webkit-line-clamp: 2;
    overflow: hidden;
    width: 100%;
    border: none;
    background: transparent;
    color: inherit;
    cursor: pointer;
    text-align: left;
    text-decoration: none;
    transition: color var(--transition-fast), border-color var(--transition-fast);
  }

  :global(.article-toc__section) {
    padding: 0 0 0 0.9rem;
    border-left: 2px solid transparent;
    font-family: var(--font-body);
    font-size: var(--text-base);
    font-weight: 700;
    line-height: 1.4;
    color: var(--text-secondary);
  }

  :global(.article-toc__section:hover),
  :global(.article-toc__section--active) {
    color: var(--text);
    border-left-color: var(--text);
  }

  :global(.article-toc__subs) {
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
    margin-left: 0.9rem;
    padding-left: 0.9rem;
    border-left: 1px solid var(--border);
  }

  :global(.article-toc__sub) {
    font-family: var(--font-editorial);
    font-size: var(--text-sm);
    line-height: 1.46;
    color: var(--dim);
  }

  :global(.article-toc__sub:hover),
  :global(.article-toc__sub--active) {
    color: var(--text);
  }

  :global(.article-toc__footer) {
    padding-top: 0.9rem;
    border-top: 1px solid var(--border);
  }

  :global(.article-toc__footer a) {
    text-decoration: none;
  }

  :global(.article-toc__footer a:hover) {
    color: var(--text);
    text-decoration: underline;
  }

  :global(.article-toc-mobilebar) {
    display: none;
  }

  :global(.article-toc-overlay) {
    position: fixed;
    inset: 0;
    z-index: 1000;
    background: rgba(0, 0, 0, 0.35);
    display: flex;
    align-items: flex-end;
    justify-content: center;
    padding: 1rem;
  }

  :global(.article-toc-drawer) {
    width: min(100%, 40rem);
    max-height: min(78vh, 42rem);
    overflow-y: auto;
    border-radius: 0;
    background: var(--bg);
    padding: 1.25rem 1rem 1.5rem;
    box-shadow: var(--shadow-lg);
  }

  :global(.article-toc-drawer__header) {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 1rem;
    margin-bottom: 1rem;
    padding-bottom: 0.9rem;
    border-bottom: 1px solid var(--border);
  }

  :global(.article-toc-drawer__close) {
    border: none;
    background: transparent;
    color: var(--dim);
    cursor: pointer;
    padding: 0.6rem;
    font-size: var(--text-xl);
    line-height: 1;
  }

  :global(.article-toc-drawer__entries) {
    display: flex;
    flex-direction: column;
    gap: 0.9rem;
  }

  :global(.article-toc-drawer__group) {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  :global(.article-toc-drawer__section),
  :global(.article-toc-drawer__sub) {
    display: block;
    width: 100%;
    border: none;
    background: transparent;
    color: inherit;
    cursor: pointer;
    text-align: left;
    text-decoration: none;
  }

  :global(.article-toc-drawer__section) {
    padding: 0.7rem 0 0.7rem 0.7rem;
    min-height: 44px;
    border-left: 1px solid transparent;
    border-radius: 0;
    font-family: var(--font-body);
    font-size: var(--text-base);
    font-weight: 700;
    line-height: 1.4;
    color: var(--text);
  }

  :global(.article-toc-drawer__section--active) {
    background: none;
    border-left-color: var(--text);
  }

  :global(.article-toc-drawer__sub) {
    padding: 0.45rem 0.7rem 0.45rem 1.4rem;
    min-height: 36px;
    font-family: var(--font-editorial);
    font-size: var(--text-sm);
    line-height: 1.46;
    color: var(--text-secondary);
  }

  :global(.article-toc-drawer__sub--active) {
    color: var(--text);
    font-weight: 700;
  }

  :global(.article-toc-drawer__footer) {
    margin-top: 1.25rem;
    padding-top: 0.9rem;
    border-top: 1px solid var(--border);
  }

  :global(.article-toc-drawer__footer a) {
    font-size: var(--text-sm);
    color: var(--dim);
    text-decoration: underline;
  }

  @media (max-width: 1279px) {

    /* Block flow, not a grid — and the aside + island shell become
       display:contents — so the sticky Contents bar's containing block is
       the whole article instead of a wrapper exactly as tall as the bar
       itself, which is why it could never actually stick. */
    .article-grid--wide {
      display: block;
    }

    .article-grid__toc {
      position: static;
      top: auto;
      display: contents;
    }

    :global(.article-toc-shell) {
      display: contents;
    }

    :global(.article-toc) {
      display: none;
    }

    :global(.article-toc-mobilebar) {
      display: block;
      position: sticky;
      top: 0;
      z-index: 20;
      background: var(--bg);
      margin-bottom: 1.75rem;
    }

    /* A printed running head, not app chrome: house caps recipe over a
       single hairline. Never gains a shadow, blur, translucency, progress
       indicator, or share affordance. */
    :global(.article-toc-mobilebar__button) {
      width: 100%;
      display: flex;
      align-items: baseline;
      gap: 0.5rem;
      padding: 0.7rem 0 0.6rem;
      border: none;
      border-bottom: 1px solid var(--border);
      border-radius: 0;
      background: transparent;
      box-shadow: none;
      color: var(--dim);
      cursor: pointer;
      text-align: left;
    }

    :global(.article-toc-mobilebar__label) {
      display: block;
      min-width: 0;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      font-size: var(--text-xs);
      font-family: var(--font-body);
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: var(--dim);
    }

    :global(.article-toc-mobilebar__caret) {
      display: inline-block;
      margin-right: 0.35rem;
      font-size: 0.8em;
      transition: transform 0.2s ease;
      transform-origin: 50% 55%;
    }

    :global(.article-toc-mobilebar__caret--open) {
      transform: rotate(90deg);
    }

    :global(.article-toc-mobilebar__current) {
      color: var(--text);
    }
  }

  @media (max-width: 600px) {
    .article-page--standard {
      max-width: 100%;
    }

    .article-header {
      margin-bottom: 2rem;
      padding-bottom: 1.25rem;
    }

    .article-header__title {
      font-size: var(--text-display);
      line-height: 1.06;
    }

    .article-header__meta {
      gap: 0.45rem 0.75rem;
      font-size: var(--text-xs);
    }

    .article-header__meta-item {
      gap: 0.75rem;
    }

    .article-header__standfirst {
      font-size: var(--text-lg);
      line-height: var(--leading-ui);
      margin-top: 1.15rem;
    }

    .article-prose {
      font-size: var(--text-md);
      line-height: var(--leading-prose);
    }

    .article-prose > :global(p:first-child) {
      font-size: 1.12em;
    }

    .article-prose :global(h1) {
      font-size: var(--text-2xl);
      margin-top: 3rem;
    }

    .article-prose :global(h2) {
      font-size: var(--text-xl);
      margin-top: 2.4rem;
    }

    .article-prose :global(h3) {
      font-size: var(--text-lg);
      margin-top: 1.85rem;
    }

  }

  /* ═══ TOC / Wide Layout ═══ */
  .cheat-sheet-page {
    width: min(1200px, 100vw - 48px);
    margin-left: calc((min(1200px, 100vw - 48px) - 100%) / -2);
  }

  .toc-page .prose :global(h1),
  .toc-page .prose :global(h2),
  .toc-page .prose :global(h3) {
    scroll-margin-top: 24px;
  }

  /* Section-level headings: accent border, large */
  .toc-page .prose :global(h1),
  .toc-page .prose :global(h2) {
    font-size: var(--text-2xl);
    font-weight: 700;
    line-height: 1.2;
    letter-spacing: -0.01em;
    color: var(--text);
    margin-top: var(--space-3xl);
    margin-bottom: var(--space-lg);
    padding-bottom: var(--space-sm);
    border-bottom: 1px solid var(--border-strong);
  }
  .toc-page .prose :global(h1:first-child),
  .toc-page .prose :global(h2:first-child) {
    margin-top: 0;
  }
  @media (max-width: 600px) {
    .toc-page .prose :global(h1),
    .toc-page .prose :global(h2) { font-size: var(--text-xl); }
  }

  /* Sub-level headings: accent color, subtler border */
  .toc-page .prose :global(h3) {
    font-size: var(--text-md);
    font-weight: 700;
    line-height: 1.35;
    color: var(--accent);
    margin-top: 2rem;
    margin-bottom: var(--space-lg);
    padding-bottom: var(--space-xs);
    border-bottom: 1px solid var(--border);
  }
  @media (max-width: 600px) {
    .toc-page .prose :global(h3) { font-size: var(--text-base); }
  }

  :global(.cse-layout) {
    display: grid; grid-template-columns: 240px 1fr; gap: 3rem; align-items: start;
  }
  @media (max-width: 900px) {
    :global(.cse-layout) { grid-template-columns: 1fr; }
  }
  :global(.cse-sidebar) {
    position: sticky; top: 16px; max-height: calc(100vh - 32px);
    overflow-y: auto; overflow-x: hidden; padding-bottom: 16px;
    -webkit-overflow-scrolling: touch;
  }
  :global(.cse-sidebar::-webkit-scrollbar) { width: 2px; }
  :global(.cse-sidebar::-webkit-scrollbar-thumb) { background: var(--border); border-radius: 2px; }
  :global(.cse-sidebar::-webkit-scrollbar-track) { background: transparent; }
  @media (max-width: 900px) {
    :global(.cse-sidebar) { display: none; }
  }
  :global(.cse-sidebar__header) {
    font-size: var(--text-xs); font-weight: 700; text-transform: uppercase;
    letter-spacing: 0.12em; color: var(--dim); padding-bottom: 8px;
    margin-bottom: 4px; border-bottom: 1px solid var(--border);
  }
  :global(.cse-sidebar__entries) { display: flex; flex-direction: column; gap: 2px; }
  :global(.cse-sidebar__group) { margin-bottom: 2px; }
  :global(.cse-sidebar__section) {
    display: block; width: 100%; text-align: left; padding: 5px 8px;
    border: none; border-radius: 0; background: transparent; cursor: pointer;
    font-family: inherit; font-size: var(--text-xs); font-weight: 700;
    color: var(--text); line-height: 1.35; transition: color 0.1s, background 0.1s;
  }
  :global(.cse-sidebar__section:hover) { background: transparent; color: var(--accent); }
  :global(.cse-sidebar__section--active) { color: var(--accent); background: transparent; }
  :global(.cse-sidebar__subs) {
    padding-left: 12px; margin-left: 6px; border-left: 1px solid var(--border); margin-bottom: 6px;
  }
  :global(.cse-sidebar__sub) {
    display: block; width: 100%; text-align: left; padding: 3px 6px;
    border: none; border-radius: 0; background: transparent; cursor: pointer;
    font-family: inherit; font-size: var(--text-xs); color: var(--dim);
    line-height: 1.35; transition: color 0.1s;
  }
  :global(.cse-sidebar__sub:hover) { color: var(--text); }
  :global(.cse-sidebar__sub--active) { color: var(--accent); font-weight: 700; }
  :global(.cse-sidebar__back) {
    margin-top: 12px; padding-top: 10px; border-top: 1px solid var(--border);
  }
  :global(.cse-sidebar__back a) { font-size: var(--text-xs); color: var(--dim); text-decoration: none; }
  :global(.cse-sidebar__back a:hover) { color: var(--accent); text-decoration: underline; }
  :global(.cse-content) { min-width: 0; max-width: 780px; }
  :global(.cse-intro) {
    margin-bottom: var(--space-2xl); padding: 0 0 0 var(--space-xl);
    background: transparent; border-radius: 0; border-left: 1px solid var(--border);
  }
  :global(.cse-section) { margin-bottom: var(--space-3xl); scroll-margin-top: 24px; }
  :global(.cse-section__label) {
    font-size: var(--text-xs); font-weight: 700; text-transform: uppercase;
    letter-spacing: 0.12em; color: var(--dim); margin-bottom: 6px; font-family: var(--font-label);
  }
  :global(.cse-section__title) {
    font-size: var(--text-2xl); font-weight: 700; line-height: 1.2; letter-spacing: -0.01em;
    color: var(--text); margin-bottom: var(--space-lg); padding-bottom: var(--space-sm);
    border-bottom: 1px solid var(--border-strong);
  }
  @media (max-width: 600px) {
    :global(.cse-section__title) { font-size: var(--text-xl); }
  }
  :global(.cse-section__intro) { margin-bottom: var(--space-xl); }
  :global(.cse-section__arguments) { display: flex; flex-direction: column; gap: var(--space-xl); }
  :global(.cse-argument) {
    position: relative; padding: var(--space-lg) var(--space-xl);
    background: transparent; border: 1px solid var(--border); border-radius: 0;
    scroll-margin-top: 24px; transition: border-color 0.2s, background-color 0.2s;
  }
  :global(.cse-argument:hover) { border-color: var(--border-strong); background: var(--bg-subtle); box-shadow: none; }
  :global(.cse-argument__number) {
    position: absolute; top: var(--space-lg); right: var(--space-lg);
    font-size: var(--text-xs); font-family: var(--font-label); font-weight: 700; color: var(--dim); opacity: 0.5;
  }
  :global(.cse-argument__title) {
    font-size: var(--text-md); font-weight: 700; line-height: 1.35; color: var(--accent);
    margin-bottom: var(--space-lg); padding-right: 2.5rem; padding-bottom: var(--space-sm);
    border-bottom: 1px solid var(--border-subtle);
  }
  @media (max-width: 600px) {
    :global(.cse-argument__title) { font-size: var(--text-base); }
  }
  :global(.cse-argument__body) { font-size: var(--font-size-base); line-height: var(--leading-prose); }
  :global(.cse-back-to-top) {
    position: fixed; bottom: 76px; right: 20px; z-index: 998;
    width: 36px; height: 36px; border-radius: 0; background: var(--bg);
    color: var(--dim); border: 1px solid var(--border); cursor: pointer; font-size: var(--text-base);
    display: flex; align-items: center; justify-content: center;
    box-shadow: none; transition: color 0.1s, border-color 0.1s, background-color 0.1s;
  }
  :global(.cse-back-to-top:hover) { color: var(--accent); border-color: var(--accent); background: var(--bg-subtle); }
  :global(.cse-mobile-toggle) {
    display: none; position: fixed; bottom: 20px; right: 20px; z-index: 999;
    width: 54px; height: 44px; border-radius: 0; background: var(--bg);
    color: var(--accent); border: 1px solid var(--accent); cursor: pointer; box-shadow: none;
    align-items: center; justify-content: center; flex-direction: column; gap: 1px;
    transition: transform var(--transition-fast);
  }
  :global(.cse-mobile-toggle:active) { transform: scale(0.92); }
  :global(.cse-mobile-pct) { font-size: var(--text-xs); font-weight: 700; letter-spacing: 0.02em; opacity: 0.8; }
  @media (max-width: 900px) { :global(.cse-mobile-toggle) { display: flex; } }
  @media (min-width: 901px) { :global(.cse-mobile-toggle) { display: none !important; } }
  :global(.cse-mobile-overlay) {
    position: fixed; inset: 0; z-index: 1000; background: rgba(0, 0, 0, 0.5);
    display: flex; align-items: flex-end; justify-content: center;
  }
  :global(.cse-mobile-toc) {
    background: var(--bg); border-radius: 0; width: 100%;
    max-height: 80vh; overflow-y: auto;
    padding: var(--space-xl) var(--space-lg) var(--space-2xl); -webkit-overflow-scrolling: touch;
  }
  :global(.cse-mobile-toc__header) {
    display: flex; justify-content: space-between; align-items: center;
    margin-bottom: var(--space-lg); padding-bottom: var(--space-sm);
    border-bottom: 1px solid var(--border); font-size: var(--text-sm); font-weight: 700; color: var(--text);
  }
  :global(.cse-mobile-toc__close) { background: none; border: none; color: var(--dim); cursor: pointer; padding: 4px; }
  :global(.cse-mobile-toc__entries) { display: flex; flex-direction: column; gap: 2px; }
  :global(.cse-mobile-toc__group) { margin-bottom: 8px; }
  :global(.cse-mobile-toc__section) {
    display: block; width: 100%; text-align: left; padding: 8px 10px;
    border: none; border-radius: 0; background: transparent; cursor: pointer;
    font-family: inherit; font-size: var(--text-base); font-weight: 700; color: var(--text); line-height: 1.35;
  }
  :global(.cse-mobile-toc__section--active) { color: var(--accent); background: transparent; }
  :global(.cse-mobile-toc__sub) {
    display: block; width: 100%; text-align: left; padding: 5px 10px 5px 20px;
    border: none; background: transparent; cursor: pointer;
    font-family: inherit; font-size: var(--text-sm); color: var(--dim); line-height: 1.4;
  }
  :global(.cse-mobile-toc__sub--active) { color: var(--accent); font-weight: 700; }

  /* Cheat sheet prose overrides */
  .cheat-sheet-page :global(.prose img) { max-width: 100%; height: auto; margin: var(--space-xl) 0; }
  .cheat-sheet-page :global(.prose figure) { margin: var(--space-xl) 0; }
  .cheat-sheet-page :global(.prose figcaption) { font-size: var(--text-sm); color: var(--dim); text-align: center; margin-top: var(--space-sm); }
  .cheat-sheet-page :global(.prose blockquote) {
    border-left: 3px solid var(--accent); padding-left: var(--space-lg);
    margin: var(--space-xl) 0; color: var(--dim); font-style: italic;
  }
  .cheat-sheet-page :global(.prose blockquote > :last-child) { margin-bottom: 0; }
  .cheat-sheet-page :global(.prose a) {
    color: var(--accent); text-decoration: underline; text-underline-offset: var(--underline-offset);
  }
  .cheat-sheet-page :global(.prose h1),
  .cheat-sheet-page :global(.prose h2),
  .cheat-sheet-page :global(.prose h3) { scroll-margin-top: 20px; color: var(--text); }
  .cheat-sheet-page :global(.prose h1) {
    font-size: var(--text-display);
    font-weight: 700;
    line-height: 1.12;
    letter-spacing: -0.01em;
    margin-top: 4rem;
    margin-bottom: 1rem;
  }
  .cheat-sheet-page :global(.prose h1:first-child),
  .cheat-sheet-page :global(.prose h2:first-child) { margin-top: 0; }
  @media (max-width: 600px) {
    .cheat-sheet-page :global(.prose h1) { font-size: var(--text-2xl); margin-top: 3rem; }
    .cheat-sheet-page :global(.prose h2) { font-size: var(--text-xl); margin-top: 2.4rem; }
    .cheat-sheet-page :global(.prose h3) { font-size: var(--text-base); margin-top: 1.85rem; }
  }
  .cheat-sheet-page :global(.prose p) { margin-bottom: var(--space-lg); }
  .cheat-sheet-page :global(.prose ul),
  .cheat-sheet-page :global(.prose ol) { margin-bottom: var(--space-lg); padding-left: var(--space-xl); }
  .cheat-sheet-page :global(.prose li) { margin-bottom: var(--space-sm); }
  .cheat-sheet-page :global(.prose .image-link),
  .cheat-sheet-page :global(.prose .image2) { display: block; margin: var(--space-xl) 0; }
  .cheat-sheet-page :global(.prose .image-link img),
  .cheat-sheet-page :global(.prose .image2 img) { max-width: 100%; height: auto; }
  .cheat-sheet-page :global(.prose .captioned-image-container) { margin: var(--space-xl) 0; }
  .cheat-sheet-page :global(.prose .image-caption) { font-size: var(--text-sm); color: var(--dim); text-align: center; margin-top: var(--space-sm); }
  .cheat-sheet-page :global(.prose .subscription-widget-wrap),
  .cheat-sheet-page :global(.prose .subscribe-widget),
  .cheat-sheet-page :global(.prose .subscription-widget),
  .cheat-sheet-page :global(.prose .image-link-expand),
  .cheat-sheet-page :global(.prose .icon-container) { display: none !important; }
  .cheat-sheet-page :global(.prose .footnote-anchor) { font-size: 0.7em; vertical-align: super; line-height: 0; text-decoration: none; color: var(--accent); }
  .cheat-sheet-page :global(.prose .footnote-anchor:hover) { text-decoration: underline; }
  .cheat-sheet-page :global(.prose .footnotes),
  .cheat-sheet-page :global(.prose .footnote) {
    font-size: var(--text-sm); color: var(--dim); margin-top: var(--space-2xl);
    padding-top: var(--space-lg); border-top: 1px solid var(--border);
  }
  .cheat-sheet-page :global(.prose .footnote .footnote-number) { font-size: 0.85em; margin-right: var(--space-sm); color: var(--accent); text-decoration: none; }
  .cheat-sheet-page :global(.prose pre) { background: var(--border); padding: var(--space-lg); border-radius: 0; overflow-x: auto; font-family: var(--font-mono); font-size: var(--text-sm); }
  .cheat-sheet-page :global(.prose code) { font-family: var(--font-mono); font-size: var(--text-sm); background: var(--border); padding: 0.1em 0.3em; border-radius: 0; }
  .cheat-sheet-page :global(.prose pre code) { background: none; padding: 0; }
</style>

<script>
  // In-column footnote expansion below the sidenote gate (the same 1280px
  // matchMedia the sidenotes use, so the two mechanisms never overlap).
  // The anchor stays a true link to the bottom list — new-tab, no-JS, and
  // print all land there; a plain click expands the note in place instead
  // of teleporting a 39-minute essay to its end. Delegated once; survives
  // View Transitions without stacking listeners. Capture phase: Astro's
  // ClientRouter also delegates clicks and honors defaultPrevented, so our
  // preventDefault must run before its bubble-phase listener or the router
  // scrolls to the bottom list anyway.
  if (!(window as any).__footnoteExpandBound) {
    (window as any).__footnoteExpandBound = true;
    const wide = window.matchMedia('(min-width: 1280px)');

    document.addEventListener('click', (e) => {
      if (wide.matches) return;
      const anchor = (e.target as HTMLElement).closest?.('a.footnote-anchor');
      if (!anchor) return;
      if (anchor.closest('.cheat-sheet-page')) return;
      if (!anchor.closest('.article-prose')) return;

      const href = anchor.getAttribute('href') || '';
      const noteId = href.startsWith('#') ? href.slice(1) : '';
      if (!noteId) return;
      const numberEl = document.getElementById(noteId);
      const note = numberEl?.closest('.footnote');
      const content = note?.querySelector('.footnote-content');
      if (!content) return;

      e.preventDefault();

      // Nearest block-level ancestor — anchors live inside <li> and
      // <blockquote> too, not just paragraphs.
      let block: HTMLElement | null = anchor.parentElement;
      while (block && getComputedStyle(block).display === 'inline') {
        block = block.parentElement;
      }
      if (!block) return;

      const existing = block.nextElementSibling;
      if (existing && existing.getAttribute('data-footnote-inline') === noteId) {
        existing.remove();
        return;
      }
      // A different expanded note directly after this block gets replaced.
      if (existing && existing.hasAttribute('data-footnote-inline')) {
        existing.remove();
      }

      const clone = content.cloneNode(true) as HTMLElement;
      clone.querySelectorAll('[id]').forEach(el => el.removeAttribute('id'));
      const wrap = document.createElement('aside');
      wrap.className = 'footnote-inline';
      wrap.setAttribute('data-footnote-inline', noteId);
      wrap.append(...Array.from(clone.childNodes));
      block.after(wrap);
    }, true);
  }
</script>
