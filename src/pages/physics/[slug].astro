---
import Base from '@/layouts/Base.astro';
import { getCollection, type CollectionEntry } from 'astro:content';

export async function getStaticPaths() {
  const items = await getCollection('physics');
  return items.map(item => ({
    params: { slug: item.slug },
    props: { item },
  }));
}

interface Props {
  item: CollectionEntry<'physics'>;
}

const { item } = Astro.props;
const { Content } = await item.render();
---

<Base title={item.data.title} description={item.data.description} keywords={item.data.tags}>
  <article class="container container--narrow">
    
    <header class="physics-header animate-in">
      {item.data.unit && (
        <span class="physics-header__unit">{item.data.unit}</span>
      )}
      <h1 class="physics-header__title">{item.data.title}</h1>
      {item.data.topic && (
        <p class="physics-header__topic">{item.data.topic}</p>
      )}
    </header>

    {item.data.youtube_url && (
      <div class="video-embed animate-in">
        <a href={item.data.youtube_url} target="_blank" rel="noopener" class="video-link">
          <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <polygon points="5 3 19 12 5 21 5 3"></polygon>
          </svg>
          Watch on YouTube
        </a>
      </div>
    )}

    <div class="prose animate-in">
      <Content />
    </div>
    
    <footer class="article-footer">
      <nav class="article-nav">
        <a href="/physics" class="article-nav__link">
          <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="m12 19-7-7 7-7"></path>
            <path d="M19 12H5"></path>
          </svg>
          Back to physics archive
        </a>
      </nav>
    </footer>
  </article>
</Base>

<style>
  .physics-header {
    margin-bottom: var(--space-2xl);
  }
  
  .physics-header__unit {
    display: inline-block;
    font-size: var(--font-size-mono);
    font-family: var(--font-label);
    font-weight: var(--font-weight-heading);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--accent);
    background: none;
    padding: 0 0 2px;
    border-bottom: 1px solid var(--accent);
    border-radius: 0;
    margin-bottom: var(--space-md);
  }

  .physics-header__title {
    font-family: var(--font-editorial);
    font-size: clamp(2rem, 1.7rem + 0.65vw, 2.55rem);
    font-weight: 600;
    line-height: 1.08;
    letter-spacing: -0.03em;
    margin: 0 0 var(--space-sm) 0;
  }

  .physics-header__topic {
    font-size: var(--font-size-body);
    color: var(--text-secondary);
    margin: 0;
  }

  .video-embed {
    margin-bottom: var(--space-2xl);
  }

  .video-link {
    display: inline-flex;
    align-items: center;
    gap: var(--space-sm);
    padding: 0.75rem 1rem;
    background: transparent;
    color: var(--accent);
    text-decoration: none;
    border: 1px solid var(--accent);
    border-radius: 0;
    font-weight: 500;
    transition: color var(--transition-fast), background-color var(--transition-fast), border-color var(--transition-fast);
  }

  .video-link:hover {
    background: var(--accent-subtle);
    color: var(--accent-hover);
    border-color: var(--accent-hover);
  }

  .article-footer {
    margin-top: var(--space-3xl);
    padding-top: var(--space-xl);
    border-top: 1px solid var(--border);
  }

  .article-nav__link {
    display: inline-flex;
    align-items: center;
    gap: var(--space-sm);
    font-size: var(--font-size-ui);
    color: var(--text-secondary);
    text-decoration: none;
    padding: 0;
    margin-left: 0;
    border-radius: 0;
    border-bottom: 1px solid transparent;
    transition: color var(--transition-fast), border-color var(--transition-fast);
  }
  
  .article-nav__link:hover {
    color: var(--text-primary);
    border-bottom-color: var(--accent);
  }
</style>
