import * as THREE from 'three';
import {buildBathhouse,BATHHOUSE as row} from '../../src/lib/town/bathhouse';
const renderer=new THREE.WebGLRenderer({antialias:true});renderer.setSize(innerWidth,innerHeight);renderer.setPixelRatio(1);renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.shadowMap.enabled=true;renderer.shadowMap.type=THREE.PCFSoftShadowMap;document.body.append(renderer.domElement);
const scene=new THREE.Scene();scene.background=new THREE.Color('#cad5d6');const center=new THREE.Vector3(-744,row.floor,492),building=buildBathhouse(center,0);scene.add(building.group);
scene.add(new THREE.HemisphereLight('#e3edf3','#747360',1.6));const sun=new THREE.DirectionalLight('#fff4dc',2.25);sun.position.set(-20,50,35);sun.castShadow=true;sun.shadow.mapSize.set(2048,2048);Object.assign(sun.shadow.camera,{left:-40,right:40,top:40,bottom:-40,near:.1,far:150});sun.shadow.bias=-.00015;scene.add(sun);
const ground=new THREE.Mesh(new THREE.PlaneGeometry(180,180),new THREE.MeshStandardMaterial({color:'#8a8e73',roughness:1}));ground.rotation.x=-Math.PI/2;ground.position.y=-.17;ground.receiveShadow=true;scene.add(ground);
const camera=new THREE.PerspectiveCamera(48,innerWidth/innerHeight,.05,300);const params=new URLSearchParams(location.search),view=params.get('view')??'front',positions:{[k:string]:number[]}={front:[-33,6,-37],side:[25,7,-30],rear:[34,7,32],roof:[-20,35,-24]};camera.position.fromArray(positions[view]);camera.lookAt(-1,1.7,0);renderer.render(scene,camera);(window as any).__gallery={ready:true,view,triangles:building.triangles,draws:renderer.info.render.calls,scope:'Isolated geometry/material view; level terrain is a diagnostic backdrop, not actual game terrain.'};
